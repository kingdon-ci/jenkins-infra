apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy 
metadata:
  name: policy-www
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: www-image
  filterTags:
    pattern: '^master-(?P<ts>[0-9]+)'
    extract: '$ts'
  policy:
    numerical:
      order: asc
