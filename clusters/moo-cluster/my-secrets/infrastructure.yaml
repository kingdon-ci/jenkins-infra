---
apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: my-secrets
  namespace: flux-system
spec:
  suspend: false
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg
  interval: 30m0s
  path: ./secrets/moo-cluster
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
