apiVersion: apps/v1
kind: Deployment
metadata:
  name: podinfo
spec:
  template:
    spec:
      containers:
        - name: podinfod
          image: 6.0.1 # {"$imagepolicy": "flux-system:podinfo-dev"}
